when@dev:
    web_profiler:
        toolbar: false

    framework:
        profiler:
            collect_serializer_data: false
            collect: false

when@test:
    framework:
        profiler: { collect: false }
