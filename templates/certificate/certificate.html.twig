<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate of Completion</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Montserrat:wght@300;400;600;700&display=swap');
        
        @page {
            size: A4;
            margin: 15mm;
        }
        
        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        
        .certificate-container {
            width: 100%;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            box-sizing: border-box;
        }
        
        .certificate {
            width: 100%;
            max-width: 900px;
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            border: 12px solid transparent;
            border-image: linear-gradient(45deg, #f39c12, #e74c3c, #3498db, #2ecc71) 1;
            border-radius: 20px;
            padding: 50px 60px;
            text-align: center;
            position: relative;
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
            overflow: hidden;
        }
        
        .certificate::before {
            content: '';
            position: absolute;
            top: -8px;
            left: -8px;
            right: -8px;
            bottom: -8px;
            background: linear-gradient(45deg, #f39c12, #e74c3c, #3498db, #2ecc71);
            border-radius: 16px;
            z-index: -1;
            opacity: 0.1;
        }
        
        .certificate::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="%23e9ecef" stroke-width="0.5" opacity="0.3"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            opacity: 0.05;
            z-index: -1;
        }
        
        .certificate-header {
            margin-bottom: 30px;
            position: relative;
        }
        
        .certificate-title {
            font-family: 'Playfair Display', serif;
            font-size: 42px;
            font-weight: 900;
            color: #2c3e50;
            text-transform: uppercase;
            letter-spacing: 4px;
            margin-bottom: 8px;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.1);
            position: relative;
        }
        
        .certificate-title::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: linear-gradient(90deg, #f39c12, #e74c3c);
            border-radius: 2px;
        }
        
        .certificate-subtitle {
            font-size: 18px;
            color: #6c757d;
            font-weight: 300;
            margin-bottom: 25px;
            font-style: italic;
        }
        
        .certificate-body {
            margin: 30px 0;
            position: relative;
            z-index: 2;
        }
        
        .recipient-name {
            font-family: 'Playfair Display', serif;
            font-size: 32px;
            font-weight: 700;
            color: #2c3e50;
            margin: 25px 0;
            padding: 15px 30px;
            border-bottom: 3px solid #e74c3c;
            display: inline-block;
            background: linear-gradient(135deg, rgba(243, 156, 18, 0.1) 0%, rgba(231, 76, 60, 0.1) 100%);
            border-radius: 8px;
        }
        
        .certificate-text {
            font-size: 16px;
            line-height: 1.7;
            color: #495057;
            margin: 25px 0;
            font-weight: 400;
        }
        
        .course-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 22px;
            font-weight: 700;
            color: #e74c3c;
            margin: 20px 0;
            text-transform: uppercase;
            letter-spacing: 2px;
            padding: 15px 25px;
            background: linear-gradient(135deg, #e74c3c, #f39c12);
            color: white;
            border-radius: 10px;
            display: inline-block;
        }
        
        .certificate-details {
            display: flex;
            justify-content: space-between;
            margin: 35px 0;
            padding: 25px;
            background: rgba(52, 152, 219, 0.1);
            border-radius: 15px;
            border: 1px solid rgba(52, 152, 219, 0.2);
        }
        
        .detail-item {
            text-align: center;
            flex: 1;
        }
        
        .detail-label {
            font-size: 12px;
            color: #6c757d;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            font-weight: 600;
        }
        
        .detail-value {
            font-size: 18px;
            font-weight: 700;
            color: #2c3e50;
        }
        
        .score-badge {
            display: inline-block;
            padding: 15px 30px;
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            color: white;
            font-size: 22px;
            font-weight: 700;
            border-radius: 30px;
            margin: 25px 0;
            box-shadow: 0 8px 25px rgba(46, 204, 113, 0.4);
            position: relative;
            overflow: hidden;
        }
        
        .score-badge::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            animation: shine 3s infinite;
        }
        
        @keyframes shine {
            0% { transform: translateX(-100%); }
            50% { transform: translateX(100%); }
            100% { transform: translateX(100%); }
        }
        
        .certificate-footer {
            margin-top: 50px;
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
        }
        
        .signature-section {
            text-align: center;
            width: 45%;
            position: relative;
        }
        
        .signature-line {
            border-bottom: 2px solid #2c3e50;
            margin: 35px 0 8px 0;
            height: 40px;
            position: relative;
        }
        
        .signature-line::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 30px;
            height: 2px;
            background: linear-gradient(90deg, #3498db, #2ecc71);
            border-radius: 1px;
        }
        
        .signature-text {
            font-size: 12px;
            color: #6c757d;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 600;
        }
        
        .certificate-date {
            text-align: center;
            margin-top: 35px;
            font-size: 16px;
            color: #495057;
            font-weight: 600;
        }
        
        .seal {
            position: absolute;
            bottom: 25px;
            right: 25px;
            width: 90px;
            height: 90px;
            border: 4px solid #e74c3c;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #ffffff, #f8f9fa);
            font-weight: 700;
            color: #e74c3c;
            font-size: 11px;
            text-align: center;
            transform: rotate(-12deg);
            box-shadow: 0 8px 20px rgba(231, 76, 60, 0.3);
            z-index: 10;
        }
        
        .seal-inner {
            width: 75%;
            height: 75%;
            border: 2px solid #e74c3c;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: white;
        }
        
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 140px;
            color: rgba(44, 62, 80, 0.08);
            font-weight: 900;
            font-family: 'Montserrat', sans-serif;
            pointer-events: none;
            z-index: 1;
            text-transform: uppercase;
            letter-spacing: 8px;
        }
        
        .validation-code {
            position: absolute;
            top: 20px;
            right: 20px;
            background: linear-gradient(135deg, #3498db, #2ecc71);
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 10px;
            font-weight: 600;
            font-family: 'Courier New', monospace;
            letter-spacing: 1px;
            box-shadow: 0 4px 10px rgba(52, 152, 219, 0.3);
        }

        /* Print-specific styles */
        @media print {
            body {
                background: white;
            }
            
            .certificate-container {
                padding: 0;
                min-height: auto;
            }
            
            .certificate {
                box-shadow: none;
                border: 12px solid #2c3e50;
                background: white;
            }
            
            .certificate::before,
            .certificate::after {
                display: none;
            }
            
            .validation-code {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="certificate-container">
        <div class="certificate">
            <div class="watermark">UNILEARN</div>
            
            <!-- Validation Code -->
            <div class="validation-code">CERT-{{ course.id }}-{{ user.id }}-{{ completionDate|date('Y-m-d') }}</div>
            
            <div class="certificate-header">
                <h1 class="certificate-title">Certificate of Achievement</h1>
                <p class="certificate-subtitle">This is to proudly certify that</p>
            </div>
            
            <div class="certificate-body">
                <h2 class="recipient-name">{{ user.fullName }}</h2>
                
                <p class="certificate-text">
                    has successfully demonstrated mastery and completed the comprehensive course
                </p>
                
                <h3 class="course-title">{{ course.title }}</h3>
                
                <div class="score-badge">
                    Excellence Score: {{ percentage }}%
                </div>
                
                <p class="certificate-text">
                    with outstanding performance and has proven exceptional understanding of the subject matter.
                </p>
                
                <div class="certificate-details">
                    <div class="detail-item">
                        <div class="detail-label">Course ID</div>
                        <div class="detail-value">#{{ course.id }}</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Completion Date</div>
                        <div class="detail-value">{{ completionDate|date('F j, Y') }}</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Final Score</div>
                        <div class="detail-value">{{ score }}/{{ maxScore }}</div>
                    </div>
                </div>
            </div>
            
            <div class="certificate-footer">
                <div class="signature-section">
                    <div class="signature-line"></div>
                    <div class="signature-text">Director of Education</div>
                </div>
                
                <div class="signature-section">
                    <div class="signature-line"></div>
                    <div class="signature-text">Academic Director</div>
                </div>
            </div>
            
            <div class="certificate-date">
                Officially issued on {{ completionDate|date('F j, Y') }}
            </div>
            
            <div class="seal">
                <div class="seal-inner">
                    UNILEARN<br>CERTIFIED
                </div>
            </div>
        </div>
    </div>
</body>
</html>
